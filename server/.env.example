# the port your server will run on
PORT=5000

# your MongoDB connection string (local or MongoDB Atlas)
MONGO_URI=mongodb://127.0.0.1:27017/service-board-placeholder

# secret key for signing JSON Web Tokens (should be a long random string in production)
JWT_SECRET=your_jwt_secret_placeholder
